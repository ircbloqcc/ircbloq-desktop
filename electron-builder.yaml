directories:
  buildResources: buildResources
  output: dist
extraResources: ["tools", "external-resources", 'firmwares', "statics", "drivers"]

appId: ircbloq.cc.ircbloq-desktop
productName: "IrcBloq"
publish: # empty provider list = don't publish

fileAssociations:
  ext: ib
  name: Ircbloq project file
  role: Editor
  icon: buildResources/IrcBloqFile.ico

# mac:
#   category: public.app-category.education
#   entitlements: buildResources/entitlements.mac.plist
#   extendInfo:
#     NSCameraUsageDescription: >-
#         This app requires camera access when taking a photo in the paint editor or using the video sensing blocks.
#     NSMicrophoneUsageDescription: >-
#         This app requires microphone access when recording sounds or detecting loudness.
#   gatekeeperAssess: true
#   hardenedRuntime: true
#   icon: buildResources/IrcBloqDesktop.icns
#   provisioningProfile: embedded.provisionprofile
#   artifactName: "IrcBloq ${version}.${ext}"
#   target:
#     - dmg
#     - mas
# dmg:
#   title: "IrcBloq ${version}"
# mas:
#   category: public.app-category.education
#   entitlements: buildResources/entitlements.mas.plist
#   entitlementsInherit: buildResources/entitlements.mas.inherit.plist
#   hardenedRuntime: false
#   icon: buildResources/IrcBloqDesktop.icns
# masDev:
#   type: development
#   provisioningProfile: mas-dev.provisionprofile
win:
  icon: buildResources/IrcBloqDesktop.ico
  target:
    # - appx
    - nsis
# appx:
#   identityName: "IrcbloqTeam.IrcbloqDesktop
#   publisherDisplayName: "Ircbloq Team"
#   publisher: "CN=2EC43DF1-469A-4119-9AB9-568A0A1FF65F"
#   artifactName: "Ircbloq ${version}.${ext}"
nsis:
  oneClick: false # allow user to choose per-user or per-machine
  allowToChangeInstallationDirectory: true
  include: buildResources/installer.nsh
#   license: LICENSE
  artifactName: "IrcBloq ${version} Setup.${ext}"
